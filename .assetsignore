# Cloudflare Pages serves only the static site (index.html). Everything else in
# this repo is ingestion tooling or regenerable data, not web content.
# Pages Functions under functions/ are deployed separately and are NOT affected
# by this file.
*.py
*.md
*.npy
*.jsonl
LICENSE
.gitignore
.assetsignore
.github/
__pycache__/
.DS_Store
_text/
_index/
tsd-boe-data/
boarddocs_unids.json
